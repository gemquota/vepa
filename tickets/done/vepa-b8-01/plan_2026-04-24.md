# Plan: VEPA-B8-01 - 4 Presets Implementation

## 1. Goal
Implement the 4 presets (VOID_CORE, NEURAL_DRIFT, SOLAR_FLARE, CRYSTAL_LATTICE) as defined in `rrp_synthesis.md`.

## 2. Implementation Steps

### 2.1 Update `physics.worker.js` (Core Logic)
- [x] Add `radiationGrid` as a global variable.
- [x] Implement `pure.void`: Linear Evaporative Decay for large masses.
- [x] Implement `pure.rad`: Emission of radiation into the grid and its effect on reproduction.
- [x] Implement `pure.bond`: Elastic tethers and link decay.
- [x] Implement `dna.conductivity`: Exchange of internal energy during collisions.
- [x] Implement `dna.elasticity`: Support for "Elastic Rebound" (reflecting kinetic energy).

### 2.2 Update `persistenceEngine.js` (Presets)
- [x] Update `getDefaultPresets()` to correctly define the 4 presets with their unique DNA and flags.
- [x] Expand `createSet` to support more parameters or manually define the 4 presets.

### 2.3 Update `ui.js` / `constants.js` (UI/Metadata)
- [x] Ensure new laws are available in the UI for toggling.

### 2.4 Cleanup Tickets
- [x] Move all "Done" or "Completed" tickets to `tickets/done`.

## 3. Verification Plan
- [ ] Run the engine and load each preset.
- [ ] Confirm mass loss in `VOID_CORE`.
- [ ] Confirm radiation grid is populated in `SOLAR_FLARE`.
- [ ] Confirm tether behavior in `NEURAL_DRIFT`.
- [ ] Confirm conductive flow in `CRYSTAL_LATTICE`.

Plan created.
