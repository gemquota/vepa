# Law Info UI Expansion Implementation Plan

## Overview
Transform the "Parameter Intel" module into a high-fidelity "LAW INFO" center with complex SVG icons, graphic intros, and multi-layered detail.

## Scope Definition (CRITICAL)
### In Scope
- Renaming the module to "LAW INFO".
- Creating a library of complex SVG icons for each simulation law.
- Implementing a graphic introductory header for the info module.
- Surface multiple layers of `HELP_DB` in the UI for increased detail.
- Styling the new layout in `style.css`.

### Out of Scope (DO NOT TOUCH)
- Implementing the physics for new biology triggers (vepa-b7-02).
- Fixing flickering (vepa-b7-03).

## Current State Analysis
- `src/ui.js:203`: `renderInfoModule` logic.
- `src/constants.js`: `HELP_DB` source.
- `style.css:160`: `info-module-container` styling.

## Implementation Phases

### Phase 1: Icon Library & Graphic Intro
- **Goal**: Define SVG icons and the introductory header.
- **Steps**:
  1. [ ] Define a `LAW_ICONS` object in `src/ui.js` with complex SVG strings.
  2. [ ] Add an `introGraphic` template to `renderInfoModule`.
- **Verification**: UI shows a new header and placeholder icons.

### Phase 2: Expanded Detail Rendering
- **Goal**: Update the rendering loop to show more info and the new icons.
- **Steps**:
  1. [x] Modify `renderGroup` in `src/ui.js` to include the SVG icon.
  2. [x] Update `info-item-desc` to include `explanation`, `system`, and `advanced` layers from `HELP_DB`.
- **Verification**: Each law item shows an icon and a multi-paragraph description.

### Phase 3: Visual Polish (CSS)
- **Goal**: Style the icons and the new graphic layout.
- **Steps**:
  1. [x] Update `style.css` with styles for `.law-icon`, `.info-intro`, and expanded descriptions.
  2. [ ] Ensure the "LAW INFO" rename is applied to the toggle button.
- **Verification**: UI matches the "high-fidelity" aesthetic requested.
