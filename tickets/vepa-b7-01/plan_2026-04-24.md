# Biology Expansion & Law Reorganization Plan

## Overview
Reorganize simulation toggles into "Pure" (Physics) and "Biol" (Biology) groups. Expand biological triggers to support more complex emergence.

## Scope Definition (CRITICAL)
### In Scope
- Reorganizing `VepaEngine.laws` into `pure` and `biol` groups.
- Updating `ui.js` to reflect the new grouping.
- Adding sub-trigger toggles for biology.
- Updating `physics.worker.js` to handle the new laws structure.

### Out of Scope (DO NOT TOUCH)
- Implementing the actual logic for tracking/senescence (Ticket vepa-b7-02).
- Fixing the physics flickering (Ticket vepa-b7-03).

## Current State Analysis
- `src/main.js:18`: `this.laws` is a flat object.
- `src/ui.js:210`: `renderInfoModule` uses a hardcoded `lawMap`.
- `src/worker/physics.worker.js:84`: Accesses `laws.grav`, `laws.drag`, etc.

## Implementation Phases

### Phase 1: Engine Schema Update
- **Goal**: Reorganize `this.laws` and update methods that modify it.
- **Steps**:
  1. [x] Update `VepaEngine` constructor in `src/main.js` to group laws.
  2. [x] Update `toggleLaw`, `updatePhysics`, and `syncUI` calls to handle nested paths or flattened keys.
- **Verification**: Sim starts without errors; `engine.laws` has the new structure.

### Phase 2: Worker Logic Update
- **Goal**: Update physics worker to read laws from the new structure.
- **Steps**:
  1. [x] Update `src/worker/physics.worker.js` to check `laws.pure.*` and `laws.biol.*` (or keep flat for worker and only nest in UI/Engine).
- **Verification**: Sim behavior (gravity, life) remains functional.

### Phase 3: UI Grouping
- **Goal**: Render laws in two distinct groups in the UI.
- **Steps**:
  1. [x] Update `renderInfoModule` in `src/ui.js` to support grouped rendering.
  2. [x] Add toggles for `affinity`, `reproduction`, `tracking`, `senescence`, `genotype`, `phenotype`.
- **Verification**: UI shows two groups: "PURE PHYSICS" and "BIOLOGICAL TRIGGERS".
