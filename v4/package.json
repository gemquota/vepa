{
  "name": "vepa-v4",
  "version": "4.1.2",
  "description": "VEPA v4 \u2014 Vector Emergent Physics Automata (Integrated Intelligence release)",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "syntax-check": "find src -name '*.js' -exec node --check {} +"
  },
  "dependencies": {
    "pixi.js": "^8.18.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "vite": "^8.0.8",
    "vitest": "^3.2.7"
  }
}
